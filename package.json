{
  "name": "prac",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@cap-js/hana": "^2",
    "@sap/cds": "^9"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2.4"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "cds": {
    "requires": {
      "db": "hana"
    },
    "protocols": {
      "odata": {
        "path": "/odata/v4/catalog",
        "cors": {
          "origin": "*"
        }
      }
    }
  }
}